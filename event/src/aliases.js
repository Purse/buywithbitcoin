import { fetchUsername, fetchCartItems, fetchAddToList } from './actions/index';

export default {
  'GET_USERNAME': fetchUsername,
  'GET_CART_ITEMS': fetchCartItems,
  'ADD_TO_CART': fetchAddToList
}